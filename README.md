# WebCrew Landing Page

Deployed to [webcrew.app](https://webcrew.app) via GitHub Actions → Cloudflare Pages.

## Public pricing flag

Pricing tiers and their navigation links are hidden by default. Set
`NEXT_PUBLIC_SHOW_PRICING=true` in the build environment to publish them.
